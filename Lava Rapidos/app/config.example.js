// Copie este arquivo para config.js e preencha com os dados do seu projeto Supabase.
// (Project Settings -> API). A anon key é pública por design e protegida pelo RLS.
// O arquivo config.js está no .gitignore — não suba credenciais por engano.
window.LAVA_CONFIG = {
  SUPABASE_URL: "https://SEU-PROJETO.supabase.co",
  SUPABASE_ANON_KEY: "SUA_ANON_KEY_AQUI",
};
